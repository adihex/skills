# HUB-246/HUB-247/HUB-248 Herdr dogfood evidence

Date: 2026-08-25. Source: today's real dispatch transcripts/status captures supplied in the task brief; installed CLI checks retained in `/tmp/herdr-integration-status.txt`, `/tmp/pi-launch-help.txt`, `/tmp/pi-status-help.txt`, and `/tmp/herdr-prompt-help.txt`.

| ID | Taxonomy | Evidence and root cause | Durable rule added |
|---|---|---|---|
| 1 | wrapper/setup mismatch | `pi-team-herdr launch --workspace <existing-id>` returned `workspace setup failed`; native `herdr agent start --pane` worked. Current wrapper and native help were inspected; root cause is not proven beyond wrapper/setup-path mismatch. | Stop blind retries; use native start or launch without workspace and inspect. |
| 2 | configuration drift | Requested `cvf/luna • medium` became `openai-codex/gpt-5.6-luna • high` after preset extension load. | Verify live footer provider/model/thinking/context after launch. |
| 3 | restart/session preservation | Removing preset required agent restart. Three workers died after extension/config changes and resumed with `pi --continue` in the same worktree. | Restart only after same project/worktree check; verify workspace, footer, state, and artifacts. |
| 4 | control-plane timeout | `herdr agent prompt ... --wait` timed out at 30s while accepted prompts continued. | Prefer nonblocking submit for long work; classify timeout separately from worker failure. |
| 5 | CLI documentation drift | Bare wrapper `status` is rejected; current help requires `--manifest`. | Document `status --manifest`; use native `herdr agent list` for live state. |
| 6 | integration skew | Pi integration was v2 while Herdr expected v8. | Preflight `herdr integration status`/doctor, install Pi integration, restart agents. |
| 7 | shared-extension collision | `herdr integration install omp` refused because OMP and Pi share the extension directory. | Record collision; do not force migration. |
| 8 | worktree readiness | workmux `post_create pnpm install` failed on canvas/pangocairo; `--no-hooks` worktrees lacked node_modules until later. | Treat setup and readiness separately; record concrete dependency/readiness checks. |
| 9 | skill source collision | Work-profile copies duplicated canonical `~/.agents/skills`; symlinks were required. | Resolve canonical source and avoid editing installed/generated copies. |

Golden scenarios: not applicable. Only documentation changed; `team.ts` and runtime were untouched. If runtime changes, rerun G1-G10 per pi-dogfood-os.
